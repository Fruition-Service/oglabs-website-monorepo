import type { Redirect } from "next/dist/lib/load-custom-routes"

// OG Labs redirects — add new entries here.
export const redirects: Redirect[] = []

// Root export for next.config.ts
export const wixRedirects: Redirect[] = []
